<p id="notice"><%= notice %></p>

<p>
  <h2><%= @brewery.name %></h2>
</p>
<p>Number of beers: <%= @brewery.beers.count %></p>
<p>
  <i>Established year <%= @brewery.year %></i>
</p>

<p>
  <% @brewery.beers.each do |beer| %>
    <%= link_to beer.name, beer  %><br>
  <% end %>
</p>

<% if @brewery.ratings.count>0 %>
<p> brewery rating count: <%= @brewery.ratings.count %> <%= "rating".pluralize(@brewery.ratings.count) %>, average rating <%= @brewery.avg(@brewery.ratings) %></p>
<% end %>
<%= link_to 'Edit', edit_brewery_path(@brewery) %> |
<%= link_to 'Back', breweries_path %>
